// You can specify statuses which you wanna allow for specific request
exports.PERMISSIONS = Object.freeze({
    ONLY_USERS: ['VERIFIED'],
    // Just for example
    PUBLIC: ['PENDING', 'VERIFIED'],
});
